<% provide(:title, "Redigera artikel") %>
<div class="row">
	<div class="col-md-8 col-md-offset-2 primary-col">
		<h1>Redigera artikel</h1>
		<%= form_for @article do |f| %>
			<%= render 'form', f: => f %>
		<% end %>
	</div>
</div>