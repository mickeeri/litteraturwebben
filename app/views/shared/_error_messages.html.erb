<% if object.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-danger">
			<span>Formuläret innehåller <%= object.errors.count %> fel. Kontrollera och försök igen.</span>
			<ul>
				<% object.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
	</div>
<% end %>